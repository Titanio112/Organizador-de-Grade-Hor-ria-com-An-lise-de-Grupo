- generic [active] [ref=f6e1]: "// logica.js — Business logic layer (ES Module) import { subjectsData, appState, defaultSubjectsData, subjectMap } from './dados.js'; // --- Constants --- export const GRID_START_HOUR = 7; export const GRID_END_HOUR = 23; export const PIXELS_PER_HOUR = 60; export const PIXELS_PER_MINUTE = PIXELS_PER_HOUR / 60; // --- Pure helpers --- export function timeToMinutes(timeStr) { const [hours, minutes] = timeStr.split(':').map(Number); return hours * 60 + minutes; } export function findSubjectById(id) { for (const sem of subjectsData) { const found = sem.disciplinas.find(d => d.id === id); if (found) return found; } return null; } export function findSubjectByName(name) { for (const sem of subjectsData) { const found = sem.disciplinas.find(d => d.nome === name); if (found) return found; } return null; } export function getCorequisiteGroup(subject) { const group = [subject]; if (subject.correquisitos) { for (const coreqId of subject.correquisitos) { const coreq = findSubjectById(coreqId); if (coreq && !group.some(s => s.id === coreq.id)) { group.push(coreq); } } } return group; } export function getActiveProfile() { return appState.profiles.find(p => p.id === appState.activeProfileId) || appState.profiles[0]; } export function toggleSubject(subject) { const profile = getActiveProfile(); const group = getCorequisiteGroup(subject); const allSelected = group.every(s => profile.selected.includes(s.nome)); if (allSelected) { for (const s of group) { const idx = profile.selected.indexOf(s.nome); if (idx > -1) profile.selected.splice(idx, 1); } } else { for (const s of group) { if (!profile.selected.includes(s.nome)) { profile.selected.push(s.nome); } } } saveAppState(); } export function toggleHistoryStatus(id, element, name, profile) { const approvedSet = new Set(profile.history.approved); const failedSet = new Set(profile.history.failed); element.classList.remove('status-none', 'status-approved', 'status-failed'); if (approvedSet.has(id)) { approvedSet.delete(id); failedSet.add(id); element.classList.add('status-failed'); element.innerHTML = `<span>${name}</span> <span class=\"status-icon\">🔴</span>`; } else if (failedSet.has(id)) { failedSet.delete(id); element.classList.add('status-none'); element.innerHTML = `<span>${name}</span> <span class=\"status-icon\">⚪</span>`; } else { approvedSet.add(id); element.classList.add('status-approved'); element.innerHTML = `<span>${name}</span> <span class=\"status-icon\">✅</span>`; } profile.history.approved = Array.from(approvedSet); profile.history.failed = Array.from(failedSet); } export function toggleDarkMode() { appState.darkMode = !appState.darkMode; document.body.classList.toggle('dark-mode', appState.darkMode); saveAppState(); } export function toggleGroupMode() { appState.groupMode = !appState.groupMode; saveAppState(); } export function saveAppState() { localStorage.setItem('gradeOrganizer_v2', JSON.stringify(appState)); } export function loadAppState() { try { const saved = localStorage.getItem('gradeOrganizer_v2'); if (saved) { const parsed = JSON.parse(saved); Object.assign(appState, parsed); } } catch (e) { console.error('Erro ao carregar estado:', e); Object.assign(appState, { profiles: [], activeProfileId: null, darkMode: false, groupMode: false }); } } export function resetAllData() { const confirmed = confirm('Isso vai apagar TODOS os perfis, histórico e grades. Tem certeza absoluta?'); if (confirmed) { localStorage.removeItem('gradeOrganizer_v2'); location.reload(); } } export function processPastedJSON(textarea) { try { const parsed = JSON.parse(textarea.value); if (Array.isArray(parsed)) { subjectsData.length = 0; subjectsData.push(...parsed); const profile = getActiveProfile(); profile.selected = []; saveAppState(); return { success: true, message: 'JSON carregado com sucesso!' }; } else { throw new Error('JSON deve ser um array de semestres'); } } catch (e) { return { success: false, message: 'Erro no JSON: ' + e.message }; } } export function processFileJSON(file) { return new Promise((resolve) => { const reader = new FileReader(); reader.onload = (e) => { try { const parsed = JSON.parse(e.target.result); if (Array.isArray(parsed)) { subjectsData.length = 0; subjectsData.push(...parsed); const profile = getActiveProfile(); profile.selected = []; saveAppState(); resolve({ success: true, message: 'Arquivo carregado com sucesso!' }); } else { throw new Error('JSON deve ser um array de semestres'); } } catch (err) { resolve({ success: false, message: 'Erro no arquivo: ' + err.message }); } }; reader.readAsText(file); }); } export function findSubjectById(id) { for (const sem of subjectsData) { const found = sem.disciplinas.find(d => d.id === id); if (found) return found; } return null; } export function toggleSubject(subject) { const profile = getActiveProfile(); const group = getCorequisiteGroup(subject); const allSelected = group.every(s => profile.selected.includes(s.nome)); if (allSelected) { for (const s of group) { const idx = profile.selected.indexOf(s.nome); if (idx > -1) profile.selected.splice(idx, 1); } } else { for (const s of group) { if (!profile.selected.includes(s.nome)) { profile.selected.push(s.nome); } } } saveAppState(); } export function toggleHistoryStatus(id, element, name, profile) { const approvedSet = new Set(profile.history.approved); const failedSet = new Set(profile.history.failed); element.classList.remove('status-none', 'status-approved', 'status-failed'); if (approvedSet.has(id)) { approvedSet.delete(id); failedSet.add(id); element.classList.add('status-failed'); element.innerHTML = `<span>${name}</span> <span class=\"status-icon\">🔴</span>`; } else if (failedSet.has(id)) { failedSet.delete(id); element.classList.add('status-none'); element.innerHTML = `<span>${name}</span> <span class=\"status-icon\">⚪</span>`; } else { approvedSet.add(id); element.classList.add('status-approved'); element.innerHTML = `<span>${name}</span> <span class=\"status-icon\">✅</span>`; } profile.history.approved = Array.from(approvedSet); profile.history.failed = Array.from(failedSet); } export function toggleDarkMode() { appState.darkMode = !appState.darkMode; document.body.classList.toggle('dark-mode', appState.darkMode); saveAppState(); } export function toggleGroupMode() { appState.groupMode = !appState.groupMode; saveAppState(); } export function saveAppState() { localStorage.setItem('gradeOrganizer_v2', JSON.stringify(appState)); } export function loadAppState() { try { const saved = localStorage.getItem('gradeOrganizer_v2'); if (saved) { const parsed = JSON.parse(saved); Object.assign(appState, parsed); } } catch (e) { console.error('Erro ao carregar estado:', e); Object.assign(appState, { profiles: [], activeProfileId: null, darkMode: false, groupMode: false }); } } export function resetAllData() { const confirmed = confirm('Isso vai apagar TODOS os perfis, histórico e grades. Tem certeza absoluta?'); if (confirmed) { localStorage.removeItem('gradeOrganizer_v2'); location.reload(); } } export function processPastedJSON(textarea) { try { const parsed = JSON.parse(textarea.value); if (Array.isArray(parsed)) { subjectsData.length = 0; subjectsData.push(...parsed); const profile = getActiveProfile(); profile.selected = []; saveAppState(); return { success: true, message: 'JSON carregado com sucesso!' }; } else { throw new Error('JSON deve ser um array de semestres'); } } catch (e) { return { success: false, message: 'Erro no JSON: ' + e.message }; } } export function processFileJSON(file) { return new Promise((resolve) => { const reader = new FileReader(); reader.onload = (e) => { try { const parsed = JSON.parse(e.target.result); if (Array.isArray(parsed)) { subjectsData.length = 0; subjectsData.push(...parsed); const profile = getActiveProfile(); profile.selected = []; saveAppState(); resolve({ success: true, message: 'Arquivo carregado com sucesso!' }); } else { throw new Error('JSON deve ser um array de semestres'); } } catch (err) { resolve({ success: false, message: 'Erro no arquivo: ' + err.message }); } }; reader.readAsText(file); }); } export function findSubjectById(id) { for (const sem of subjectsData) { const found = sem.disciplinas.find(d => d.id === id); if (found) return found; } return null; } export function getCorequisiteGroup(subject) { const group = [subject]; if (subject.correquisitos) { for (const coreqId of subject.correquisitos) { const coreq = findSubjectById(coreqId); if (coreq && !group.some(s => s.id === coreq.id)) { group.push(coreq); } } } return group; } export function getActiveProfile() { return appState.profiles.find(p => p.id === appState.activeProfileId) || appState.profiles[0]; } export function toggleSubject(subject) { const profile = getActiveProfile(); const group = getCorequisiteGroup(subject); const allSelected = group.every(s => profile.selected.includes(s.nome)); if (allSelected) { for (const s of group) { const idx = profile.selected.indexOf(s.nome); if (idx > -1) profile.selected.splice(idx, 1); } } else { for (const s of group) { if (!profile.selected.includes(s.nome)) { profile.selected.push(s.nome); } } } saveAppState(); } export function toggleHistoryStatus(id, element, name, profile) { const approvedSet = new Set(profile.history.approved); const failedSet = new Set(profile.history.failed); element.classList.remove('status-none', 'status-approved', 'status-failed'); if (approvedSet.has(id)) { approvedSet.delete(id); failedSet.add(id); element.classList.add('status-failed'); element.innerHTML = `<span>${name}</span> <span class=\"status-icon\">🔴</span>`; } else if (failedSet.has(id)) { failedSet.delete(id); element.classList.add('status-none'); element.innerHTML = `<span>${name}</span> <span class=\"status-icon\">⚪</span>`; } else { approvedSet.add(id); element.classList.add('status-approved'); element.innerHTML = `<span>${name}</span> <span class=\"status-icon\">✅</span>`; } profile.history.approved = Array.from(approvedSet); profile.history.failed = Array.from(failedSet); } export function toggleDarkMode() { appState.darkMode = !appState.darkMode; document.body.classList.toggle('dark-mode', appState.darkMode); saveAppState(); } export function toggleGroupMode() { appState.groupMode = !appState.groupMode; saveAppState(); } export function saveAppState() { localStorage.setItem('gradeOrganizer_v2', JSON.stringify(appState)); } export function loadAppState() { try { const saved = localStorage.getItem('gradeOrganizer_v2'); if (saved) { const parsed = JSON.parse(saved); Object.assign(appState, parsed); } } catch (e) { console.error('Erro ao carregar estado:', e); Object.assign(appState, { profiles: [], activeProfileId: null, darkMode: false, groupMode: false }); } } export function resetAllData() { const confirmed = confirm('Isso vai apagar TODOS os perfis, histórico e grades. Tem certeza absoluta?'); if (confirmed) { localStorage.removeItem('gradeOrganizer_v2'); location.reload(); } } export function processPastedJSON(textarea) { try { const parsed = JSON.parse(textarea.value); if (Array.isArray(parsed)) { subjectsData.length = 0; subjectsData.push(...parsed); const profile = getActiveProfile(); profile.selected = []; saveAppState(); return { success: true, message: 'JSON carregado com sucesso!' }; } else { throw new Error('JSON deve ser um array de semestres'); } } catch (e) { return { success: false, message: 'Erro no JSON: ' + e.message }; } } export function processFileJSON(file) { return new Promise((resolve) => { const reader = new FileReader(); reader.onload = (e) => { try { const parsed = JSON.parse(e.target.result); if (Array.isArray(parsed)) { subjectsData.length = 0; subjectsData.push(...parsed); const profile = getActiveProfile(); profile.selected = []; saveAppState(); resolve({ success: true, message: 'Arquivo carregado com sucesso!' }); } else { throw new Error('JSON deve ser um array de semestres'); } } catch (err) { resolve({ success: false, message: 'Erro no arquivo: ' + err.message }); } }; reader.readAsText(file); }); } export function findSubjectById(id) { for (const sem of subjectsData) { const found = sem.disciplinas.find(d => d.id === id); if (found) return found; } return null; } export function toggleSubject(subject) { const profile = getActiveProfile(); const group = getCorequisiteGroup(subject); const allSelected = group.every(s => profile.selected.includes(s.nome)); if (allSelected) { for (const s of group) { const idx = profile.selected.indexOf(s.nome); if (idx > -1) profile.selected.splice(idx, 1); } } else { for (const s of group) { if (!profile.selected.includes(s.nome)) { profile.selected.push(s.nome); } } } saveAppState(); }"